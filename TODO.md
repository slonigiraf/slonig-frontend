1:
- When deduplicating BookSkills - add links to all correspodning BookConcepts and BookExercises, so one BookSkill, could reference several BookConcepts and BookExercises.
- When generating prexercises - feed concepts, exercises along with BookSkills to make sure that generated exercise templaes look similar to exercies in the book, make them be structure in blocks with the corresponding BookSkill
- Between 'Exercise templates' and 'Fix PreExercise erors' add button - 'Divide prexercises': should create preexercises for each step of the solution if it is multistep, while not deleting the parent prexercise
- 'Fix PreExercise erors' should also fix grammar

2:
- Use only transformation as ability modes.
- Fix BookConcepts and BookExercised - math katex and grammar.
- Make sure SkillTemplates are generated always - now it sometimes not generated at all.
- When generating SkillTemplates make sure that the title explains what the learner can learn from the exercises.
- When generating SkillTemplates make sure that the question contains the real question or task and the answer contains the real answer for that question, not some vague sentence.

- At each stage add AI validator that will judge the output and fix errors or ask to regenerate.



- Why all ai calls are so slow?!!!!!